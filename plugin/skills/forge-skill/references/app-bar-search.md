# App Bar Search

## Basic Usage

```html
<forge-app-bar-search slot="center">
  <input type="text" placeholder="Search...">
</forge-app-bar-search>
```

## Notes

- Place in the `center` slot of `<forge-app-bar>`
- Requires a child `<input>` element
