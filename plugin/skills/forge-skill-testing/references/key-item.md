# Key Item

## Basic Usage

```html
<forge-key-item color="#ff5722">Data Series</forge-key-item>
```

## Notes

- Used within `<forge-key>` container
- Use `color` attribute to set the indicator color (hex or CSS color)
- Label text goes in the default slot
