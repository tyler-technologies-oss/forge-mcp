# Tab

## Basic Usage

```html
<forge-tab>
  <forge-icon slot="leading" name="icon_name"></forge-icon>
  <span>Label</span>
</forge-tab>
```

## Notes

- Used within `<forge-tab-bar>` container
- Use `leading` slot for optional icons
- Label text in default slot
