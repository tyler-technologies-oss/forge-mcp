# Backdrop

## Basic Usage

```html
<forge-backdrop></forge-backdrop>
```

## Notes

- Creates a semi-transparent overlay behind modal content
- Typically used with dialogs, drawers, and other overlay components
- Usually controlled programmatically via the `open` property
