# Step

## Basic Usage

```html
<forge-step>
  <span>Step Two</span>
  <span slot="optional">Alternate Message</span>
</forge-step>
```

## Notes

- Used within `<forge-stepper>` container
- Default slot for the step label
- Use `optional` slot for additional text (like "Optional" indicators)
