# Paginator

## Basic Usage

```html
<forge-paginator page-size="10" total="100" page-index="0"></forge-paginator>
```

## Notes

- Use `page-size` to set items per page
- Use `total` to set total item count
- Use `page-index` to set current page (0-indexed)
- Typically placed below data tables or lists
