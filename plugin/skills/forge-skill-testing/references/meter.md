# Meter

## Basic Usage

```html
<forge-meter value="0.7" min="0" max="1">
  <label for="meter">Capacity</label>
</forge-meter>
```

## Notes

- Visual indicator for a scalar value within a known range
- Use `value`, `min`, and `max` attributes to define the range and current value
- Include a `<label>` for accessibility
