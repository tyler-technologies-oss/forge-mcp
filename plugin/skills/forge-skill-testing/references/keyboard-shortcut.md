# Keyboard Shortcut

## Basic Usage

```html
<forge-keyboard-shortcut key="ctrl+s"></forge-keyboard-shortcut>
```

## Notes

- Displays a keyboard shortcut in a styled format
- Use `key` attribute to specify the shortcut combination
- Use `+` to combine modifier keys (ctrl, alt, shift, cmd)
