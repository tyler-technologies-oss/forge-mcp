# Field

## Basic Usage

```html
<forge-field>
  <label for="field-input">Field Label</label>
  <input type="text" id="field-input" />
</forge-field>
```

## Notes

- Low-level field wrapper component
- **Prefer `<forge-text-field>`** for most use cases
- Use when you need custom field behavior not provided by text-field
