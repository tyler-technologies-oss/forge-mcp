# App Bar Menu Button

## Basic Usage

```html
<forge-app-bar-menu-button slot="start"></forge-app-bar-menu-button>
```

## Notes

- Place in the `start` slot of `<forge-app-bar>`
- Used to toggle navigation drawer visibility
