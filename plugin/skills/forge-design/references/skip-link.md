# Skip Link

## Basic Usage

```html
<forge-skip-link target="main">Skip to main content</forge-skip-link>
```

## Notes

- Accessibility feature for keyboard users to skip navigation
- Use `target` attribute to specify the target element id
- Should be the first focusable element on the page
- Becomes visible when focused
