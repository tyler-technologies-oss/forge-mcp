import { unsafeCSS, LitElement, html, nothing } from "lit";
import { property, queryAssignedNodes, customElement } from "lit/decorators.js";
import { when } from "lit/directives/when.js";
import { tylIconLogout } from "@tylertech/tyler-icons";
import { defineAvatarComponent, defineButtonComponent, defineDividerComponent, defineIconComponent, defineIconButtonComponent, definePopoverComponent, defineToolbarComponent, IconRegistry } from "@tylertech/forge";
import { createRef, ref } from "lit/directives/ref.js";
import "../theme-toggle/theme-toggle.mjs";
import "./profile-link/profile-link.mjs";
import styles from "./user-profile.scss.mjs";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __decorateClass = (decorators, target, key, kind) => {
  var result = kind > 1 ? void 0 : kind ? __getOwnPropDesc(target, key) : target;
  for (var i = decorators.length - 1, decorator; i >= 0; i--)
    if (decorator = decorators[i])
      result = (kind ? decorator(target, key, result) : decorator(result)) || result;
  if (kind && result) __defProp(target, key, result);
  return result;
};
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, "access private method"), method);
var _linkSlot, _signOutButtonSlot, _themeToggleRef, _UserProfileComponent_instances, links_get, themeToggle_get, signOutButton_get, handleSignOut_fn, handleSlotChange_fn;
const UserProfileComponentTagName = "forge-user-profile";
let UserProfileComponent = class extends LitElement {
  constructor() {
    super(...arguments);
    __privateAdd(this, _UserProfileComponent_instances);
    this.fullName = "";
    this.email = "";
    this.buttonLabel = "Open user profile";
    this.themeToggle = false;
    __privateAdd(this, _linkSlot, html`<slot name="link" id="link-slot"></slot>`);
    __privateAdd(this, _signOutButtonSlot, html`<slot name="sign-out-button-text" id="sign-out-button-slot">Sign Out</slot>`);
    __privateAdd(this, _themeToggleRef, createRef());
  }
  render() {
    return html`
      <forge-icon-button theme="app-bar" aria-label="${this.buttonLabel}" id="popover-trigger">
        <forge-avatar .text=${this.fullName} id="button-avatar"></forge-avatar>
      </forge-icon-button>
      <forge-popover
        id="user-profile-popover"
        anchor="popover-trigger"
        placement="bottom-end"
        arrow
        position-strategy="fixed"
        @slotchange=${__privateMethod(this, _UserProfileComponent_instances, handleSlotChange_fn)}>
        <div class="user-info-container">
          <forge-avatar
            .text=${this.fullName}
            class="popover-avatar"
            id="popover-avatar"></forge-avatar>
          <div class="user-info">
            <div class="full-name">${this.fullName}</div>
            <div class="email">${this.email}</div>
          </div>
        </div>
        ${when(this._slottedLinkNodes.length, () => html`<forge-divider></forge-divider>`)} 
        ${__privateGet(this, _UserProfileComponent_instances, links_get)}
        ${__privateGet(this, _UserProfileComponent_instances, themeToggle_get)} 
        ${__privateGet(this, _UserProfileComponent_instances, signOutButton_get)}
      </forge-popover>
    `;
  }
  /** Sets the theme for the theme toggle. */
  setTheme(value) {
    if (__privateGet(this, _themeToggleRef).value) {
      __privateGet(this, _themeToggleRef).value.setTheme(value);
    }
  }
};
_linkSlot = /* @__PURE__ */ new WeakMap();
_signOutButtonSlot = /* @__PURE__ */ new WeakMap();
_themeToggleRef = /* @__PURE__ */ new WeakMap();
_UserProfileComponent_instances = /* @__PURE__ */ new WeakSet();
links_get = function() {
  const showLinks = this._slottedLinkNodes.length > 0;
  return when(
    showLinks,
    () => html` <forge-list>${__privateGet(this, _linkSlot)}</forge-list> `,
    () => __privateGet(this, _linkSlot)
  );
};
themeToggle_get = function() {
  const showThemeToggle = this.themeToggle;
  return when(
    showThemeToggle,
    () => html`
        <forge-divider></forge-divider>
        <div class="theme-toggle-container">
          <forge-theme-toggle ${ref(__privateGet(this, _themeToggleRef))}></forge-theme-toggle>
        </div>
      `,
    () => nothing
  );
};
signOutButton_get = function() {
  return html`
      <forge-toolbar inverted>
        <div slot="end">
          <forge-button class="sign-out-button" id="sign-out-button" @click=${__privateMethod(this, _UserProfileComponent_instances, handleSignOut_fn)}>
            ${__privateGet(this, _signOutButtonSlot)}
            <forge-icon name="logout" external slot="end"></forge-icon>
          </forge-button>
        </div>
      </forge-toolbar>
    `;
};
handleSignOut_fn = function() {
  const event = new Event("forge-user-profile-sign-out", {
    bubbles: true,
    composed: true
  });
  this.dispatchEvent(event);
};
handleSlotChange_fn = function(evt) {
  const slotName = evt.target.name;
  if (["profile-button-text", "link", "sign-out-button-text"].includes(slotName)) {
    this.requestUpdate();
  }
};
defineAvatarComponent();
defineButtonComponent();
defineDividerComponent();
defineIconComponent();
defineIconButtonComponent();
definePopoverComponent();
defineToolbarComponent();
IconRegistry.define([tylIconLogout]);
UserProfileComponent.styles = unsafeCSS(styles);
__decorateClass([
  property({ attribute: "full-name" })
], UserProfileComponent.prototype, "fullName", 2);
__decorateClass([
  property()
], UserProfileComponent.prototype, "email", 2);
__decorateClass([
  property({ attribute: "button-label" })
], UserProfileComponent.prototype, "buttonLabel", 2);
__decorateClass([
  property({ type: Boolean, attribute: "theme-toggle" })
], UserProfileComponent.prototype, "themeToggle", 2);
__decorateClass([
  queryAssignedNodes({ slot: "link", flatten: true })
], UserProfileComponent.prototype, "_slottedLinkNodes", 2);
UserProfileComponent = __decorateClass([
  customElement(UserProfileComponentTagName)
], UserProfileComponent);
export {
  UserProfileComponent,
  UserProfileComponentTagName
};
