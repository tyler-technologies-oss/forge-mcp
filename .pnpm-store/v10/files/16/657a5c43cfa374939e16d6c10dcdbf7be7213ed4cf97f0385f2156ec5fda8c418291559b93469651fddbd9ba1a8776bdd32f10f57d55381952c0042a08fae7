import { unsafeCSS, LitElement, html } from "lit";
import { property, queryAssignedElements, customElement } from "lit/decorators.js";
import { defineTextFieldComponent, defineIconButtonComponent, defineIconComponent, IconRegistry } from "@tylertech/forge";
import { tylIconMinus, tylIconPlus } from "@tylertech/tyler-icons";
import styles from "./quantity-field.scss.mjs";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __decorateClass = (decorators, target, key, kind) => {
  var result = kind > 1 ? void 0 : kind ? __getOwnPropDesc(target, key) : target;
  for (var i = decorators.length - 1, decorator; i >= 0; i--)
    if (decorator = decorators[i])
      result = (kind ? decorator(target, key, result) : decorator(result)) || result;
  if (kind && result) __defProp(target, key, result);
  return result;
};
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), member.set(obj, value), value);
var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, "access private method"), method);
var _internals, _QuantityFieldComponent_instances, tryGetInput_fn, onDecrement_fn, onIncrement_fn;
const QuantityFieldComponentTagName = "forge-quantity-field";
let QuantityFieldComponent = class extends LitElement {
  constructor() {
    super();
    __privateAdd(this, _QuantityFieldComponent_instances);
    this.invalid = false;
    this.required = false;
    this.decrementLabel = "Decrement";
    this.incrementLabel = "Increment";
    __privateAdd(this, _internals);
    __privateSet(this, _internals, this.attachInternals());
  }
  willUpdate(changedProperties) {
    if (changedProperties.has("required")) {
      if (this.required) {
        __privateGet(this, _internals).states.add("required");
      } else {
        __privateGet(this, _internals).states.delete("required");
      }
    }
    if (changedProperties.has("invalid")) {
      if (this.invalid) {
        __privateGet(this, _internals).states.add("invalid");
      } else {
        __privateGet(this, _internals).states.delete("invalid");
      }
    }
  }
  render() {
    return html`
      <div class="container">
        <slot name="label"></slot>
        <div class="inner">
          <slot name="decrement-button" @click=${__privateMethod(this, _QuantityFieldComponent_instances, onDecrement_fn)}>
            <forge-icon-button shape="squared" aria-label=${this.decrementLabel}>
              <slot name="decrement-icon">
                <forge-icon name="minus"></forge-icon>
              </slot>
            </forge-icon-button>
          </slot>
          <forge-text-field .invalid=${this.invalid} .required=${this.required}>
            <slot></slot>
          </forge-text-field>
          <slot name="increment-button" @click=${__privateMethod(this, _QuantityFieldComponent_instances, onIncrement_fn)}>
            <forge-icon-button shape="squared" aria-label=${this.incrementLabel}>
              <slot name="increment-icon">
                <forge-icon name="plus"></forge-icon>
              </slot>
            </forge-icon-button>
          </slot>
        </div>
        <slot name="support-text"></slot>
      </div>
    `;
  }
};
_internals = /* @__PURE__ */ new WeakMap();
_QuantityFieldComponent_instances = /* @__PURE__ */ new WeakSet();
tryGetInput_fn = function() {
  return this._defaultSlotElements.find((el) => el.tagName === "INPUT");
};
onDecrement_fn = function() {
  const input = __privateMethod(this, _QuantityFieldComponent_instances, tryGetInput_fn).call(this);
  input?.stepDown();
  input?.dispatchEvent(new Event("input", { bubbles: true, composed: true }));
  input?.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
};
onIncrement_fn = function() {
  const input = __privateMethod(this, _QuantityFieldComponent_instances, tryGetInput_fn).call(this);
  input?.stepUp();
  input?.dispatchEvent(new Event("input", { bubbles: true, composed: true }));
  input?.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
};
defineTextFieldComponent();
defineIconButtonComponent();
defineIconComponent();
IconRegistry.define([tylIconMinus, tylIconPlus]);
QuantityFieldComponent.styles = unsafeCSS(styles);
__decorateClass([
  property({ type: Boolean })
], QuantityFieldComponent.prototype, "invalid", 2);
__decorateClass([
  property({ type: Boolean })
], QuantityFieldComponent.prototype, "required", 2);
__decorateClass([
  property({ attribute: "decrement-label" })
], QuantityFieldComponent.prototype, "decrementLabel", 2);
__decorateClass([
  property({ attribute: "increment-label" })
], QuantityFieldComponent.prototype, "incrementLabel", 2);
__decorateClass([
  queryAssignedElements()
], QuantityFieldComponent.prototype, "_defaultSlotElements", 2);
QuantityFieldComponent = __decorateClass([
  customElement(QuantityFieldComponentTagName)
], QuantityFieldComponent);
export {
  QuantityFieldComponent,
  QuantityFieldComponentTagName
};
